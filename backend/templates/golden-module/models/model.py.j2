from typing import TYPE_CHECKING

from sqlalchemy import String, CheckConstraint, Index
from sqlalchemy.orm import Mapped, mapped_column, relationship

from app.database.base import Base
from app.models.base_mixins import TimestampMixin, SoftDeleteMixin

if TYPE_CHECKING:
    pass


class {{module_name}}(Base, TimestampMixin, SoftDeleteMixin):
    __tablename__ = "{{table_name}}"
    __table_args__ = (
        Index("ix_{{table_name}}_active", "active"),
    )

    id: Mapped[int] = mapped_column(primary_key=True, autoincrement=True)
    {{model_fields}}

    def __repr__(self) -> str:
        return f"<{{module_name}}(id={self.id}, {{repr_field}}={{repr_expr}})>"
